# Generate inputs
from generate_graphs import create_random_graph, create_branching_graph, create_cycle_graph, create_diamond_graph
from subgraph import join_subgraphs


def generate_graph(size):


if __name__ == '__main__':
    G = generate_graph(50)
    G.draw()

def write_to_file():
